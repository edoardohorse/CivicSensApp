<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorSecondary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="CardView.Report" parent="CardView">
        <item name="cardBackgroundColor">@android:color/white</item>
        <item name="cardCornerRadius">0dp</item>
        <item name="cardMaxElevation">1dp</item>
        <item name="cardElevation">0.7dp</item>
        <item name="contentPadding">10dp</item>
        <!--<item name="cardPreventCornerOverlap">true</item>-->
        <item name="cardUseCompatPadding">true</item>
    </style>

    <style name="CardView.Photo" parent="CardView.Report">
        <item name="cardCornerRadius">5dp</item>
        <item name="cardElevation">2dp</item>
        <item name="android:minWidth">200dp</item>
        <item name="android:layout_height">match_parent</item>
        <!--<item name="android:drawable">@drawable/ic_add_24dp</item>-->
        <item name="android:icon">@drawable/ic_add_24dp</item>"
    </style>


    <string-array name="grade_report">
        <item>Basso</item>
        <item>Medio</item>
        <item>Alto</item>
    </string-array>

</resources>


